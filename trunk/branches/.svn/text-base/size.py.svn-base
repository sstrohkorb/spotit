"""
This class sets up the image locations, size, and rotation for both cards
"""

import pygame
import random

class Size(object):
	
	def __init__(self, left_card_points, right_card_points):
		self.left_card_points = left_card_points
		self.right_card_points = right_card_points

	def initial_point(self, self.left_card_points, self.right_card_points)
		self.left_card_points = []
		self.right_card_points = []
		lc_points = ([225,75],[335,115],[375,225],[335,335],[225,375],[115,335],[75,225],
					[115,115])
		rc_points = ([725,75],[835,115],[875,225],[835,335],[725,375],[615,335],[575,225],
					[615,115])
		a = random.randint(0,7)
		b = random.randint(0,7)
		left_card_points.append(lc_points[a])
		right_card_pointsd.append(rc_points[b])



#	pygame.transform.rotozoom(Surface, angle, scale): 
#		return Surface
				

	
